#include <cstdio>

int main()
{
    char a[105] = {}, b[105] = {};
    scanf("%s %s", a, b);
    int len1 = 0, len2 = 0;
    for (int i = 0; a[i] != 0; i++)
        len1++;
    for (int i = 0; b[i] != 0; )
}
